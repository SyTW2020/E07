language: node_js
node_js:
  - stable
cache:
  directories:
    - node_modules
before_script:
  - cd frontend/
# install:
  # - npm install -g @vue/cli-service
  # - @vue/cli-plugin-unit-jest
script:
  - npm install -g @vue/cli
  - npm install
  - npm run test:unit
on:
  branch: ci/paula